[workspace]
resolver = "2"
members = ["pandora", "audio", "engine"]

# Keeping the protocol crate (`pandora`, portable) and the decoder (`audio`, Windows-only) as
# separate members means the protocol work stays platform-independent and testable anywhere.
